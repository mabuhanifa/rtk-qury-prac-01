import './spinner.css';

const Spinner = () => {
    return (
        <div class="center">
         <div class="ring"></div>
         <span>loading...</span>
      </div>
    );
};

export default Spinner;